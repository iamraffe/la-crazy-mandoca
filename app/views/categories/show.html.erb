<div class="col-sm-7">
    <div class="row">
      <div class="main-wrap">
        <% @posts.each do |post| %>
          <%= render partial: "posts/#{post.postable_type.to_s.downcase}_post", locals: {post: post} %>
        <% end %>
      </div>
    </div>
</div>
